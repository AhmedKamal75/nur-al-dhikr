node_modules/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea/
coverage/
*.local
test-results/
playwright-report/
# Packaging artifacts (built by npm run compress-data, shipped in the zip)
/data/**/*.gz
*.gz
# Seed/slim bundles (built by npm run build-seed, never committed)
*.zip
# Reviewer-side clutter: external audit docs + OS zone markers.
# Never committed, never bundled (build-seed honors --exclude-standard).
AUDIT-REPORT*
*:Zone.Identifier
